---
phase: 64-yard-structs-yard-cli-audit-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - yard-structs/src/config.rs
  - yard-structs/src/trigger.rs
  - yard-structs/src/state.rs
  - yard-structs/src/diff.rs
  - yard-structs/src/error.rs
  - yard-structs/src/lib.rs
autonomous: true
requirements:
  - OWN-01
  - ERR-01
  - MEM-01
  - API-01
  - ASYNC-01
  - OPT-01
  - NAME-01
  - TYPE-01
  - TEST-01
  - DOC-01
  - PERF-01
  - PROJ-01
  - LINT-01
  - ANTI-01

must_haves:
  truths:
    - "Every source file in yard-structs (6 files) has been audited against all 14 rule categories"
    - "V-01 and V-02 clippy warnings fixed: config.rs test code uses `parsed` not `&parsed` in serde_json::to_value calls"
    - "A-01 verified: AwsCredentialConfig::merge clone pattern is necessary (callers borrow, not own)"
    - "A-05 verified: mask_pii field on JobDefinition has proper serde attrs, doc, Default entry"
    - "A-06 confirmed: Source/Sink/Transform stringly-typed fields are wire format, not a violation"
    - "A-07 verified: private _AirflowSectionRaw/_AirflowJobBlockRaw have no rule violations"
    - "A-08 flagged: ProjectState.deployments uses HashMap<String, Deployment> -- serde(transparent) on JobName means JSON is identical but noted as finding"
    - "cargo clippy -p yard-structs --all-targets -- -D warnings exits 0"
    - "cargo test -p yard-structs exits 0"
  artifacts:
    - path: "yard-structs/src/config.rs"
      provides: "Fixed clippy warnings at lines 1418 and 1431 in test code"
      contains: "serde_json::to_value(parsed)"
    - path: "yard-structs/src/trigger.rs"
      provides: "Audit-verified compliance with all 14 rule categories"
    - path: "yard-structs/src/state.rs"
      provides: "Audit-verified compliance with all 14 rule categories"
    - path: "yard-structs/src/diff.rs"
      provides: "Audit-verified compliance with all 14 rule categories"
    - path: "yard-structs/src/error.rs"
      provides: "Audit-verified compliance with all 14 rule categories"
    - path: "yard-structs/src/lib.rs"
      provides: "Audit-verified compliance with all 14 rule categories"
  key_links:
    - from: "yard-structs/src/lib.rs"
      to: "yard-structs/src/config.rs"
      via: "pub use config::*"
      pattern: "pub use config"
    - from: "yard-structs/src/lib.rs"
      to: "yard-structs/src/trigger.rs"
      via: "pub use trigger::*"
      pattern: "pub use trigger"
---

<objective>
Audit all 6 source files in yard-structs against the 179 Rust coding rules (14 categories: OWN-01, ERR-01, MEM-01, API-01, ASYNC-01, OPT-01, NAME-01, TYPE-01, TEST-01, DOC-01, PERF-01, PROJ-01, LINT-01, ANTI-01) and fix all violations found.

Purpose: Third compliance pass on yard-structs, catching drift since the v1.12 audit (Phase 58, June 2026). Two substantive changes landed since then: `mask_pii: Vec<String>` field on JobDefinition (v1.14) and minor version bumps. Two confirmed clippy warnings exist (V-01, V-02).

Output: All 6 yard-structs files verified compliant. Clippy warnings fixed. Per-file audit findings documented in SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/64-yard-structs-yard-cli-audit-fix/64-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit + fix config.rs, trigger.rs, and state.rs against all 14 rule categories</name>
  <files>yard-structs/src/config.rs, yard-structs/src/trigger.rs, yard-structs/src/state.rs</files>
  <read_first>
    - yard-structs/src/config.rs (full file -- 805 prod LOC + 722 test LOC; fix V-01/V-02, verify A-01, A-05, A-06, A-07)
    - yard-structs/src/trigger.rs (full file -- 413 prod LOC + 494 test LOC)
    - yard-structs/src/state.rs (full file -- 246 prod LOC + 148 test LOC; verify A-08)
    - rules/own-borrow-over-clone.md (for A-01 assessment)
    - rules/type-no-stringly.md (for A-06 assessment)
    - rules/type-newtype-ids.md (for A-08 assessment)
  </read_first>
  <action>
    Fix V-01: In config.rs test code near line 1418, change the serde_json::to_value call to pass `parsed` instead of `&parsed`. The unnecessary borrow triggers clippy::needless_borrows_for_generic_args because serde_json::to_value accepts impl Serialize.

    Fix V-02: Same fix near line 1431 -- change `&parsed` to `parsed` in the serde_json::to_value call.

    After fixing V-01/V-02, audit all three files against all 14 rule categories. For each file, check:

    config.rs specific checks:
    - A-01: Verify AwsCredentialConfig::merge (around line 94) -- callers pass &Self references they do not own, so cloning Option<String> fields is necessary. Confirm this is not an own-borrow-over-clone violation; document the rationale.
    - A-05: Verify the mask_pii field on JobDefinition has proper doc comment (///), appropriate serde attrs (#[serde(default, skip_serializing_if = "Vec::is_empty")]), and is included in the Default impl. Confirm compliance with API-01, DOC-01, TYPE-01.
    - A-06: Verify Source/Sink/Transform fields source_type, sink_type, transform_type are String because they represent wire format values from YAML config, not because of missing type safety. Document as design choice under TYPE-01.
    - A-07: Verify _AirflowSectionRaw and _AirflowJobBlockRaw (private helper structs for custom Deserialize) have no rule violations. Private items do not require /// doc comments per doc-all-public.

    state.rs specific checks:
    - A-08: Check ProjectState.deployments field -- if it uses HashMap<String, Deployment> while JobName has #[serde(transparent)], the JSON shape would be identical if changed to HashMap<JobName, Deployment>. Document as a TYPE-01 finding but do NOT change it (backward compatibility concern with persisted state files). Flag in SUMMARY only.
    - Verify JobName::new, JobName::as_str, DagName::new, DagName::as_str have #[must_use] (added in Phase 58).
    - Verify PartialEq derives on ResourceStatus, DagState, DagDeployment (added in Phase 58).

    trigger.rs specific checks:
    - Verify dataset_uris() and source_kind() have #[must_use] (added in Phase 58).
    - Verify hand-rolled Serialize/Deserialize impls follow err-question-mark (use ? not unwrap).
    - Verify all public items have /// doc comments.

    General checks across all three files:
    - OWN-01: No unnecessary clones, borrows preferred, no &String or &Vec parameters
    - ERR-01: All fallible functions use Result/?, no unwrap in prod, # Errors doc sections present
    - MEM-01: No unnecessary format!() in non-display paths; with_capacity where size known
    - API-01: #[must_use] on pure functions, common traits derived (Debug, Clone, PartialEq where applicable)
    - NAME-01: CamelCase types, snake_case functions, SCREAMING_SNAKE constants
    - TEST-01: #[cfg(test)] mod tests with use super::*, descriptive names, arrange-act-assert
    - PERF-01: Iterators over indexing, lazy evaluation, no unnecessary collect()
    - ANTI-01: No unwrap abuse, no excessive cloning, no stringly-typed abuse

    Do NOT modify Cargo.toml. Do NOT add new external crates. Do NOT add #[non_exhaustive] to enums (workspace-internal crate per Phase 58 D-06 precedent). Do NOT change serde attributes that would alter wire format.
  </action>
  <verify>
    <automated>cd /Users/seanmcauliffe/Repos/yard-project/yard && cargo clippy -p yard-structs --all-targets -- -D warnings && cargo test -p yard-structs</automated>
  </verify>
  <acceptance_criteria>
    - config.rs line ~1418: serde_json::to_value call uses `parsed` not `&parsed`
    - config.rs line ~1431: serde_json::to_value call uses `parsed` not `&parsed`
    - cargo clippy -p yard-structs --all-targets -- -D warnings exits 0 (zero warnings including test targets)
    - cargo test -p yard-structs exits 0
    - No new unwrap() added to production code (test code may use unwrap)
    - No new unsafe blocks anywhere
    - No serde attribute changes that alter JSON/YAML wire format
  </acceptance_criteria>
  <done>config.rs V-01/V-02 clippy warnings fixed; config.rs, trigger.rs, state.rs audited against all 14 rule categories with findings documented; clippy clean on --all-targets; all tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Audit diff.rs, error.rs, lib.rs + run full gate checks</name>
  <files>yard-structs/src/diff.rs, yard-structs/src/error.rs, yard-structs/src/lib.rs</files>
  <read_first>
    - yard-structs/src/diff.rs (full file -- 73 prod LOC + 128 test LOC)
    - yard-structs/src/error.rs (full file -- 32 prod LOC, no tests)
    - yard-structs/src/lib.rs (full file -- 34 prod LOC, no tests)
    - rules/doc-all-public.md (verify all re-exports documented)
    - rules/proj-pub-use-reexport.md (verify N-01 pattern)
  </read_first>
  <action>
    Audit all three files against all 14 rule categories:

    diff.rs checks:
    - Verify DiffType and Diff derive PartialEq (added Phase 58)
    - Verify DiffType is #[non_exhaustive] (this was a deliberate choice for cross-crate consumers in yard-cli display.rs; it is the exception to the workspace-internal rule)
    - Verify all variants and fields have /// doc comments
    - Verify # Examples doc sections present on DiffType and Diff (added Phase 58)
    - Check Diff struct field types for type safety (name: String, old_hash/new_hash: Option<String>)

    error.rs checks:
    - Verify ValidationError has /// doc comment and # Examples (added Phase 58)
    - Verify Display impl follows err-lowercase-msg (lowercase, no trailing punctuation)
    - Verify the struct derives appropriate traits (Debug, Clone)

    lib.rs checks:
    - Verify #![warn(missing_docs)] is present (LINT-01)
    - Verify module-level //! documentation exists (DOC-01)
    - Verify pub use re-exports cover all public modules (N-01: proj-pub-use-reexport pattern)
    - Verify no unnecessary re-exports or missing modules

    General checks across all three files:
    - Same 14-category checklist as Task 1
    - Focus on DOC-01 (all public items documented), PROJ-01 (structure), LINT-01 (lint config)

    If any violations are found, fix them. Expected: these three small files were clean in Phase 58 and have had no changes since, so no fixes anticipated.

    After auditing, run the full gate checks to verify the entire crate is compliant.

    Do NOT modify Cargo.toml. Do NOT add #[non_exhaustive] to any additional enums beyond DiffType.
  </action>
  <verify>
    <automated>cd /Users/seanmcauliffe/Repos/yard-project/yard && cargo clippy -p yard-structs --all-targets -- -D warnings && cargo test -p yard-structs && cargo test --doc -p yard-structs</automated>
  </verify>
  <acceptance_criteria>
    - diff.rs, error.rs, lib.rs audited against all 14 rule categories
    - cargo clippy -p yard-structs --all-targets -- -D warnings exits 0
    - cargo test -p yard-structs exits 0
    - cargo test --doc -p yard-structs exits 0 (all doc examples compile and pass)
    - All 6 yard-structs files have now been audited (combined with Task 1)
    - No new unwrap() in production code
    - No new unsafe blocks
  </acceptance_criteria>
  <done>All 6 yard-structs files audited against all 14 rule categories; all gate checks pass (clippy clean on --all-targets, unit tests pass, doc tests pass); per-file findings ready for SUMMARY</done>
</task>

</tasks>

## Artifacts this phase produces

This is an AUDIT phase. Changes are minimal:

**Fixed violations:**
- `yard-structs/src/config.rs` test code: `&parsed` changed to `parsed` in two `serde_json::to_value()` calls (lines ~1418 and ~1431) to fix `clippy::needless_borrows_for_generic_args`

**Audit findings documented (no code change):**
- A-01: `AwsCredentialConfig::merge` clone pattern confirmed necessary (callers borrow)
- A-05: `mask_pii` field confirmed compliant (doc, serde attrs, Default)
- A-06: Stringly-typed wire format fields confirmed as design choice
- A-07: Private serde helper structs confirmed no violations
- A-08: `ProjectState.deployments: HashMap<String, Deployment>` flagged as TYPE-01 finding (deferred)

**Any additional violations found during audit will be fixed and listed in SUMMARY.**

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| YAML deserialization | User-provided yard.yaml/job.yaml parsed into yard-structs types |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-64-01 | Tampering | serde derives on config/state types | low | accept | This plan only fixes clippy warnings in test code and audits existing derives -- no deserialization logic is modified. deny_unknown_fields already protects against extra-field injection. |
| T-64-02 | Information Disclosure | doc examples | low | mitigate | Audit verifies existing doc examples do not embed real AWS account IDs or credentials. All examples use placeholder values (123456789012, us-east-1). |
</threat_model>

<verification>
After both tasks complete:
1. `cargo clippy -p yard-structs --all-targets -- -D warnings` exits 0
2. `cargo test -p yard-structs` exits 0
3. `cargo test --doc -p yard-structs` exits 0
4. All 6 source files have been read and checked against all 14 rule categories
5. Per-file audit findings documented in SUMMARY
</verification>

<success_criteria>
Every source file in yard-structs (6 files, ~1600 prod LOC) has been audited against all 14 rule categories. The 2 clippy warnings (V-01, V-02) are fixed. All focused audit areas (A-01, A-05, A-06, A-07, A-08) are verified and documented. Zero clippy warnings on --all-targets. All tests pass including doc tests.
</success_criteria>

<output>
Create `.planning/phases/64-yard-structs-yard-cli-audit-fix/64-01-SUMMARY.md` when done
</output>
