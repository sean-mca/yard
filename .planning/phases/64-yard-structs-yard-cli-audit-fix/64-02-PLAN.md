---
phase: 64-yard-structs-yard-cli-audit-fix
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - yard-cli/src/main.rs
  - yard-cli/src/lib.rs
  - yard-cli/src/parser.rs
  - yard-cli/src/utils.rs
  - yard-cli/src/context.rs
  - yard-cli/src/commands/mod.rs
  - yard-cli/src/commands/apply.rs
  - yard-cli/src/commands/destroy.rs
  - yard-cli/src/commands/display.rs
  - yard-cli/src/commands/force_unlock.rs
  - yard-cli/src/commands/init.rs
  - yard-cli/src/commands/list.rs
  - yard-cli/src/commands/plan.rs
  - yard-cli/src/commands/show.rs
  - yard-cli/src/commands/validate.rs
autonomous: true
requirements:
  - OWN-01
  - ERR-01
  - MEM-01
  - API-01
  - ASYNC-01
  - OPT-01
  - NAME-01
  - TYPE-01
  - TEST-01
  - DOC-01
  - PERF-01
  - PROJ-01
  - LINT-01
  - ANTI-01

must_haves:
  truths:
    - "Every source file in yard-cli (15 files) has been audited against all 14 rule categories"
    - "A-02 assessed: utils.rs colorize() evaluated for Cow return pattern (own-cow-conditional)"
    - "A-03 verified: display.rs wildcard _ => arms on DiffType match are intentional (DiffType is #[non_exhaustive])"
    - "A-04 audited: commands/list.rs rewrite (v1.14.2) fully checked against all 14 categories"
    - "cargo clippy -p yard --all-targets -- -D warnings exits 0"
    - "cargo test -p yard exits 0"
    - "cargo test --workspace exits 0 (no regressions)"
  artifacts:
    - path: "yard-cli/src/utils.rs"
      provides: "Audit-verified compliance with all 14 rule categories"
    - path: "yard-cli/src/commands/display.rs"
      provides: "Audit-verified compliance; wildcard arms confirmed intentional"
    - path: "yard-cli/src/commands/list.rs"
      provides: "Full audit of v1.14.2 rewrite against all 14 categories"
    - path: "yard-cli/src/lib.rs"
      provides: "Audit-verified; #![warn(missing_docs)] present"
    - path: "yard-cli/src/main.rs"
      provides: "Audit-verified; minimal entry point"
  key_links:
    - from: "yard-cli/src/lib.rs"
      to: "yard-cli/src/commands/mod.rs"
      via: "mod commands"
      pattern: "mod commands"
    - from: "yard-cli/src/commands/mod.rs"
      to: "yard-cli/src/commands/list.rs"
      via: "pub mod list"
      pattern: "mod list"
---

<objective>
Audit all 15 source files in yard-cli against the 179 Rust coding rules (14 categories: OWN-01, ERR-01, MEM-01, API-01, ASYNC-01, OPT-01, NAME-01, TYPE-01, TEST-01, DOC-01, PERF-01, PROJ-01, LINT-01, ANTI-01) and fix all violations found.

Purpose: Third compliance pass on yard-cli, catching drift since the v1.12 audit (Phase 58, June 2026). The main delta is the commands/list.rs rewrite (v1.14.2, ~70 LOC diff) which needs full audit attention. RESEARCH.md identified focused audit areas A-02 (colorize Cow), A-03 (display wildcard arms), A-04 (list.rs rewrite).

Output: All 15 yard-cli files verified compliant. Per-file audit findings documented in SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/64-yard-structs-yard-cli-audit-fix/64-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit + fix focus files: utils.rs, display.rs, list.rs</name>
  <files>yard-cli/src/utils.rs, yard-cli/src/commands/display.rs, yard-cli/src/commands/list.rs</files>
  <read_first>
    - yard-cli/src/utils.rs (full file -- 72 prod LOC + 56 test LOC; A-02 colorize Cow assessment)
    - yard-cli/src/commands/display.rs (full file -- 120 prod LOC + 181 test LOC; A-03 wildcard arms)
    - yard-cli/src/commands/list.rs (full file -- 88 prod LOC; A-04 full rewrite audit)
    - rules/own-cow-conditional.md (for A-02 assessment)
    - rules/own-borrow-over-clone.md (for A-02 assessment)
    - rules/anti-format-hot-path.md (for A-02 format!() usage)
  </read_first>
  <action>
    Audit these three files against all 14 rule categories. These are the files RESEARCH.md flagged for focused attention.

    utils.rs -- A-02 assessment:
    Evaluate the colorize() function which returns s.to_string() when color is disabled and uses format!() for ANSI wrapping when enabled. Assess whether Cow<str> return type would be appropriate per own-cow-conditional. Consider: (1) colorize is a CLI display helper called for terminal output, not a hot path; (2) the function signature and all callers; (3) whether the change would reduce allocations in practice. If the function is only called for terminal output lines (not in tight loops), the format!() allocation is acceptable and a Cow return would add complexity without meaningful benefit. Document assessment. If Cow IS warranted (significant allocation savings), implement the change. If not warranted, document why and leave unchanged.

    Also check utils.rs for:
    - confirm() function: verify # Errors doc section present (added Phase 58)
    - All public functions have /// doc comments
    - No unwrap() in production code
    - Parameter types: &str not &String per own-slice-over-vec

    display.rs -- A-03 verification:
    Verify the wildcard `_ =>` arms in DiffType match expressions. DiffType is #[non_exhaustive] in yard-structs, so wildcard arms are required for cross-crate consumers. Verify the fallback message is reasonable (should produce a sensible output for unknown future variants). Document that this is intentional and correct.

    Also check display.rs for:
    - print_plan_summary(): verify # Errors doc section present (added Phase 58)
    - BTreeMap usage for sorted output (PERF-01 entry-api, perf-iter-over-index)
    - Iterator patterns (no unnecessary collect, lazy where possible)
    - All public functions have /// doc comments

    list.rs -- A-04 full audit of v1.14.2 rewrite:
    This file was substantially rewritten since v1.12. Audit every line against all 14 categories:
    - ERR-01: .transpose(), .with_context() usage correct; ? propagation
    - OWN-01: Iterator chains do not clone unnecessarily; HashSet construction efficient
    - PERF-01: No unnecessary collect() calls; iterator chains are lazy where possible
    - NAME-01: Variable names follow snake_case
    - DOC-01: Public execute() function has /// doc comment with # Errors
    - ASYNC-01: Async patterns correct (tokio runtime, no lock across await)
    - ANTI-01: No unwrap abuse, no excessive cloning, no stringly-typed abuse

    Fix any violations found. Document all findings.

    Do NOT modify Cargo.toml. Do NOT add new external crates.
  </action>
  <verify>
    <automated>cd /Users/seanmcauliffe/Repos/yard-project/yard && cargo clippy -p yard --all-targets -- -D warnings && cargo test -p yard</automated>
  </verify>
  <acceptance_criteria>
    - utils.rs, display.rs, list.rs audited against all 14 rule categories
    - A-02 assessment documented (colorize Cow pattern: warranted or not, with rationale)
    - A-03 verified: wildcard arms confirmed intentional for #[non_exhaustive] DiffType
    - A-04: list.rs rewrite fully audited with per-rule findings
    - cargo clippy -p yard --all-targets -- -D warnings exits 0
    - cargo test -p yard exits 0
    - No new unwrap() in production code
    - No new unsafe blocks
  </acceptance_criteria>
  <done>Three focus files (utils.rs, display.rs, list.rs) audited against all 14 rule categories; A-02/A-03/A-04 assessments documented; any violations fixed; clippy clean; tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Audit remaining 12 yard-cli files + run full gate checks</name>
  <files>yard-cli/src/main.rs, yard-cli/src/lib.rs, yard-cli/src/parser.rs, yard-cli/src/context.rs, yard-cli/src/commands/mod.rs, yard-cli/src/commands/apply.rs, yard-cli/src/commands/destroy.rs, yard-cli/src/commands/plan.rs, yard-cli/src/commands/show.rs, yard-cli/src/commands/validate.rs, yard-cli/src/commands/init.rs, yard-cli/src/commands/force_unlock.rs</files>
  <read_first>
    - yard-cli/src/main.rs (14 LOC -- minimal entry point)
    - yard-cli/src/lib.rs (77 LOC -- run() dispatch, #![warn(missing_docs)])
    - yard-cli/src/parser.rs (142 LOC -- clap derive definitions)
    - yard-cli/src/context.rs (8 prod LOC + 151 test LOC -- re-exports)
    - yard-cli/src/commands/mod.rs (48 LOC -- module declarations, resolve_project helper)
    - yard-cli/src/commands/apply.rs (101 LOC -- apply handler)
    - yard-cli/src/commands/destroy.rs (138 LOC -- destroy handler)
    - yard-cli/src/commands/plan.rs (43 LOC -- plan handler)
    - yard-cli/src/commands/show.rs (36 LOC -- show handler)
    - yard-cli/src/commands/validate.rs (53 LOC -- validate handler)
    - yard-cli/src/commands/init.rs (55 LOC -- init handler)
    - yard-cli/src/commands/force_unlock.rs (32 LOC -- force-unlock handler)
    - rules/proj-lib-main-split.md (verify main.rs is minimal)
    - rules/err-doc-errors.md (verify # Errors on all fallible public fns)
  </read_first>
  <action>
    Audit all 12 remaining yard-cli files against all 14 rule categories. These files were verified clean in Phase 58 and have had no changes since v1.12, so expect zero violations -- but verify independently.

    main.rs checks:
    - PROJ-01: Minimal entry point, logic delegated to lib.rs run()
    - ERR-01: Error printing uses {e:#} for full anyhow chain
    - LINT-01: #![warn(clippy::unwrap_used, clippy::expect_used)] present

    lib.rs checks:
    - DOC-01: #![warn(missing_docs)] present; //! module-level doc; run() has /// doc + # Errors
    - PROJ-01: Logic split -- run() dispatches to command modules
    - ERR-01: run() returns anyhow::Result, uses ? propagation
    - LINT-01: Lint attributes present and correct

    parser.rs checks:
    - NAME-01: Type names CamelCase (Cli, Commands, ListTarget)
    - DOC-01: All public structs/enums/fields have /// doc comments
    - API-01: Clap derive usage follows conventions

    context.rs checks:
    - PROJ-01: Re-export pattern (pub use yard_core::resolve::...)
    - DOC-01: Re-exports documented
    - TEST-01: Test module follows cfg(test), use super::*, descriptive names

    commands/mod.rs checks:
    - PROJ-01: Module declarations, resolve_project helper
    - ERR-01: resolve_project returns Result with context
    - DOC-01: Public items documented

    Command handler files (apply, destroy, plan, show, validate, init, force_unlock):
    - ERR-01: All use anyhow::Result, ? propagation, .context()/.with_context()
    - ASYNC-01: async fn execute() uses tokio correctly; no lock across await
    - DOC-01: /// doc comments on execute() functions with # Errors sections
    - OWN-01: Parameters borrow where appropriate
    - ANTI-01: No unwrap in production code

    Fix any violations found during the audit. Document all per-file findings.

    After auditing all 12 files, run the full gate checks:
    1. cargo clippy -p yard --all-targets -- -D warnings
    2. cargo test -p yard
    3. cargo test --workspace (verify no cross-crate regressions)

    Do NOT modify Cargo.toml. Do NOT add new external crates.
  </action>
  <verify>
    <automated>cd /Users/seanmcauliffe/Repos/yard-project/yard && cargo clippy -p yard --all-targets -- -D warnings && cargo test -p yard && cargo test --workspace</automated>
  </verify>
  <acceptance_criteria>
    - All 12 remaining yard-cli files audited against all 14 rule categories
    - Combined with Task 1, all 15 yard-cli files now audited
    - cargo clippy -p yard --all-targets -- -D warnings exits 0
    - cargo test -p yard exits 0
    - cargo test --workspace exits 0 (no cross-crate regressions)
    - No new unwrap() in production code
    - No new unsafe blocks
    - Per-file audit findings documented for all 15 files
  </acceptance_criteria>
  <done>All 15 yard-cli files audited against all 14 rule categories; all gate checks pass (clippy clean on --all-targets, crate tests pass, workspace tests pass); per-file findings ready for SUMMARY</done>
</task>

</tasks>

## Artifacts this phase produces

This is an AUDIT phase. Expected changes are minimal to none for yard-cli (no known violations).

**Audit findings documented (code changes TBD based on audit):**
- A-02: `colorize()` in utils.rs evaluated for `Cow<str>` return type -- assessment documented
- A-03: Wildcard `_ =>` arms in display.rs confirmed intentional for `#[non_exhaustive]` DiffType
- A-04: list.rs v1.14.2 rewrite fully audited against all 14 categories
- All 15 files verified compliant or fixed

**Any violations found during audit will be fixed and listed in SUMMARY.**

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI stdin | User input via confirm() prompt in utils.rs |
| CLI stdout/stderr | Output formatting in display.rs and utils.rs |
| CLI arguments | Clap-parsed arguments in parser.rs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-64-03 | Tampering | confirm() stdin input | low | accept | This plan only audits existing code -- no input handling logic is modified. The existing confirm() reads a single line and compares against "y"/"yes". |
| T-64-04 | Information Disclosure | display.rs terminal output | low | accept | This plan only audits existing code -- no output logic is modified. Display uses color helpers without exposing sensitive data. |
| T-64-05 | Tampering | CLI argument parsing | low | accept | Clap derive macros handle argument parsing with type safety. No modifications to parser.rs expected. |
</threat_model>

<verification>
After both tasks complete:
1. `cargo clippy -p yard --all-targets -- -D warnings` exits 0
2. `cargo test -p yard` exits 0
3. `cargo test --workspace` exits 0 (full workspace green)
4. All 15 yard-cli source files have been read and checked against all 14 rule categories
5. Per-file audit findings documented in SUMMARY
</verification>

<success_criteria>
Every source file in yard-cli (15 files, ~1027 prod LOC) has been audited against all 14 rule categories. The list.rs rewrite (A-04) has received full audit attention. The colorize() Cow assessment (A-02) and display.rs wildcard arms (A-03) are documented. Any violations found are fixed. Zero clippy warnings. All tests pass including workspace-level.
</success_criteria>

<output>
Create `.planning/phases/64-yard-structs-yard-cli-audit-fix/64-02-SUMMARY.md` when done
</output>
