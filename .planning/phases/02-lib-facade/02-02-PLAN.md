---
phase: 02-lib-facade
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - yard-core/src/lib.rs
  - yard-core/src/diff.rs
  - yard-core/src/show.rs
autonomous: true
requirements: [MOD-01]

must_haves:
  truths:
    - "diff.rs contains calculate_diff and private compare_json"
    - "show.rs contains show and show_dag"
    - "lib.rs no longer contains the functions moved to diff.rs and show.rs"
    - "cargo check --workspace succeeds"
    - "All existing tests pass"
  artifacts:
    - path: "yard-core/src/diff.rs"
      provides: "Diff calculation logic"
      min_lines: 40
    - path: "yard-core/src/show.rs"
      provides: "Show/preview functions"
      min_lines: 15
  key_links:
    - from: "yard-core/src/lib.rs"
      to: "yard-core/src/diff.rs"
      via: "pub mod diff; pub use diff::calculate_diff"
      pattern: "pub use diff::calculate_diff"
    - from: "yard-core/src/lib.rs"
      to: "yard-core/src/show.rs"
      via: "pub mod show; pub use show::{show, show_dag}"
      pattern: "pub use show::"
    - from: "yard-core/src/diff.rs"
      to: "yard-core/src/codegen.rs"
      via: "use crate::codegen"
      pattern: "use crate::codegen"
    - from: "yard-core/src/show.rs"
      to: "yard-core/src/airflow_dag.rs"
      via: "use crate::airflow_dag"
      pattern: "use crate::airflow_dag"
---

<objective>
Extract diff.rs and show.rs from lib.rs. These depend only on existing crate modules (codegen, utils, airflow_dag), not on the leaf modules extracted in Plan 01, making them independent Wave 2 extractions.

Purpose: Continue reducing lib.rs toward facade-only. After this plan, only orchestrate.rs and dag_lifecycle.rs logic remains in lib.rs.
Output: Two new source files, lib.rs further reduced.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-lib-facade/02-RESEARCH.md
@.planning/phases/02-lib-facade/02-PATTERNS.md
@.planning/phases/02-lib-facade/02-01-SUMMARY.md

<interfaces>
<!-- diff.rs needs these from existing crate modules -->
From yard-core/src/codegen.rs: generate_python_script(job_name, job_def) -> Result<String>
From yard-core/src/utils.rs: calculate_hash(content: &str) -> String

<!-- show.rs needs these from existing crate modules -->
From yard-core/src/airflow_dag.rs: generate_dag(manifest, resolved_dag) -> Result<String>, ResolvedDag (struct)
From yard-core/src/codegen.rs: generate_python_script(job_name, job_def) -> Result<String>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract diff.rs and show.rs from lib.rs</name>
  <files>yard-core/src/diff.rs, yard-core/src/show.rs, yard-core/src/lib.rs</files>
  <read_first>
    - yard-core/src/lib.rs (current state after Plan 01 — see what remains)
    - .planning/phases/02-lib-facade/02-PATTERNS.md (exact line ranges, imports, function signatures)
    - .planning/phases/02-lib-facade/02-01-SUMMARY.md (what Plan 01 changed)
  </read_first>
  <action>
**Create `yard-core/src/diff.rs`:**
- Move `calculate_diff` (originally lines 143-189) and `compare_json` (originally lines 1277-1288) from lib.rs
- `calculate_diff` is `pub fn`, `compare_json` is `fn` (private)
- Add imports:
```rust
use anyhow::{Context, Result};
use serde_json::Value;
use std::collections::HashMap;
use yard_structs::{DiffType, JobDiff, ProjectManifest, ProjectState};
use crate::codegen;
use crate::utils;
```
- No tests move here — the `diff_detects_*` tests use shared helpers (`make_job`, `make_deployment`, `empty_state`, `job_hash`) and will stay centralized in orchestrate.rs (Plan 03).

**Create `yard-core/src/show.rs`:**
- Move `show` (originally lines 972-981) and `show_dag` (originally lines 967-971) from lib.rs
- Both are `pub fn`
- Add imports:
```rust
use anyhow::{Context, Result, anyhow};
use yard_structs::ProjectManifest;
use crate::airflow_dag;
use crate::codegen;
```
- `show_dag` takes `(manifest: &ProjectManifest, dags: &[airflow_dag::ResolvedDag], dag_name: &str) -> Result<String>` — uses `airflow_dag::generate_dag`
- `show` takes `(manifest: &ProjectManifest, job_name: &str) -> Result<String>` — uses `codegen::generate_python_script`
- No existing tests for show/show_dag.

**Update `yard-core/src/lib.rs`:**
- Remove the moved function bodies (calculate_diff, compare_json, show, show_dag)
- Add mod declarations: `pub mod diff;` and `pub mod show;`
- Add re-exports:
```rust
pub use diff::calculate_diff;
pub use show::{show, show_dag};
```
- Check remaining code in lib.rs that calls `calculate_diff` — it is called by `apply()` in orchestrate logic (still in lib.rs at this point). After the move, the call site in lib.rs resolves through the `pub use` re-export. Alternatively, if the remaining lib.rs code references it directly, change to `diff::calculate_diff(...)` or `crate::diff::calculate_diff(...)`.
- Remove any imports from the top of lib.rs that are now only used by the moved code (e.g., `DiffType`, `JobDiff` if not used by remaining functions — but check carefully; orchestrate code still in lib.rs likely uses `DiffType` and `JobDiff`).

**Critical: Preserve all `.context()` and `.with_context()` error messages verbatim.**
  </action>
  <verify>
    <automated>cd /Users/seanmcauliffe/Repos/yard && cargo test -p yard-core --lib && cargo check -p yard-cli -p yard-server && cargo clippy --workspace -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - yard-core/src/diff.rs exists and contains `pub fn calculate_diff(`
    - yard-core/src/diff.rs contains `fn compare_json(` (private)
    - yard-core/src/diff.rs contains `use crate::codegen`
    - yard-core/src/diff.rs contains `use crate::utils`
    - yard-core/src/show.rs exists and contains `pub fn show(`
    - yard-core/src/show.rs contains `pub fn show_dag(`
    - yard-core/src/show.rs contains `use crate::airflow_dag`
    - yard-core/src/lib.rs contains `pub mod diff;`
    - yard-core/src/lib.rs contains `pub mod show;`
    - yard-core/src/lib.rs contains `pub use diff::calculate_diff`
    - yard-core/src/lib.rs contains `pub use show::{show, show_dag}`
    - yard-core/src/lib.rs does NOT contain `fn compare_json(`
    - `cargo test -p yard-core --lib` exits 0
    - `cargo check -p yard-cli -p yard-server` exits 0
    - `cargo clippy --workspace -- -D warnings` exits 0
  </acceptance_criteria>
  <done>diff.rs and show.rs extracted. lib.rs updated with mod declarations and pub use re-exports. Full workspace compiles, all tests pass, clippy clean. Only orchestrate and dag_lifecycle logic remains in lib.rs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries affected — pure structural refactor.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | T (Tampering) | Module visibility | accept | All functions keep existing pub visibility; no new access paths. |
</threat_model>

<verification>
After this plan:
- `cargo test -p yard-core --lib` — all tests pass
- `cargo check -p yard-cli -p yard-server` — downstream consumers compile unchanged
- `cargo clippy --workspace -- -D warnings` — zero warnings
- lib.rs line count reduced by another ~70 lines (diff ~47 + show ~24)
</verification>

<success_criteria>
- diff.rs and show.rs exist as standalone modules
- lib.rs has mod declarations and pub use re-exports for both
- Zero downstream compilation changes needed
- Clippy clean
</success_criteria>

<output>
After completion, create `.planning/phases/02-lib-facade/02-02-SUMMARY.md`
</output>
