---
phase: 02-lib-facade
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - yard-core/src/lib.rs
  - yard-core/src/parsing.rs
  - yard-core/src/config_merge.rs
autonomous: true
requirements: [MOD-01]

must_haves:
  truths:
    - "parsing.rs contains all parse_* functions and private helpers (str_field, str_array_field, str_map_field, order_by_field, parse_single_source)"
    - "config_merge.rs contains is_task_only, build_provider_config, merge_provider_config"
    - "lib.rs no longer contains the functions moved to parsing.rs and config_merge.rs"
    - "All 9 config_merge tests pass in config_merge.rs"
    - "All 9 parsing tests pass in parsing.rs"
    - "cargo check --workspace succeeds"
  artifacts:
    - path: "yard-core/src/parsing.rs"
      provides: "All parse functions and private helpers"
      min_lines: 250
    - path: "yard-core/src/config_merge.rs"
      provides: "Config merge functions"
      min_lines: 40
  key_links:
    - from: "yard-core/src/lib.rs"
      to: "yard-core/src/parsing.rs"
      via: "pub mod parsing; pub use parsing::{...}"
      pattern: "pub use parsing::"
    - from: "yard-core/src/lib.rs"
      to: "yard-core/src/config_merge.rs"
      via: "pub mod config_merge; pub use config_merge::{...}"
      pattern: "pub use config_merge::"
---

<objective>
Extract the two leaf modules (parsing.rs, config_merge.rs) from lib.rs. These have zero cross-dependencies on other new modules, making them safe to extract first.

Purpose: Establish the leaf modules that orchestrate.rs and dag_lifecycle.rs will depend on in later plans.
Output: Two new source files with their tests, lib.rs updated with mod declarations and pub use re-exports for extracted functions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-lib-facade/02-RESEARCH.md
@.planning/phases/02-lib-facade/02-PATTERNS.md

<interfaces>
<!-- Key types the executor needs from yard_structs (used by parsing.rs) -->
From yard-structs (external crate, already in Cargo.toml):
- AirflowJobBlock, AirflowSection, Import, Sink, Source, Transform
- OrderBySpec (used by order_by_field)

<!-- Existing modules referenced by the extracted code -->
From yard-core/src/codegen.rs: generate_python_script (used by diff.rs, not this plan)
From yard-core/src/utils.rs: calculate_hash (used by diff.rs, not this plan)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract parsing.rs and config_merge.rs from lib.rs</name>
  <files>yard-core/src/parsing.rs, yard-core/src/config_merge.rs, yard-core/src/lib.rs</files>
  <read_first>
    - yard-core/src/lib.rs (full file — the source being decomposed)
    - .planning/phases/02-lib-facade/02-PATTERNS.md (exact line ranges and import patterns)
    - .planning/phases/02-lib-facade/02-RESEARCH.md (re-export inventory and helper mapping)
  </read_first>
  <action>
**Create `yard-core/src/config_merge.rs`:**
- Move lines 19-66 from lib.rs (functions: `is_task_only`, `build_provider_config`, `merge_provider_config`)
- Add import: `use serde_json::Value;`
- All three functions keep `pub` visibility
- Move the 9 self-contained tests from the `#[cfg(test)] mod tests` block in lib.rs: `merge_provider_config_*` (7 tests) and `is_task_only_*` (2 tests). These tests use only `super::*` and `serde_json::json!` — no shared helpers needed.
- Test module structure:
```rust
#[cfg(test)]
mod tests {
    use super::*;
    use serde_json::json;
    // ... 9 tests verbatim ...
}
```

**Create `yard-core/src/parsing.rs`:**
- Move lines 993-1276 from lib.rs (all `parse_*` functions plus private helpers)
- Public functions: `parse_body`, `parse_job_file`, `parse_airflow_section`, `parse_airflow_job_block`, `merge_airflow_sections`, `parse_partition_by`, `parse_partition_timestamp_column`, `parse_create_timestamp`, `parse_imports`, `parse_sources`, `parse_sink`, `parse_transforms`
- Private helpers (keep as `fn`, not `pub fn`): `str_field`, `str_array_field`, `str_map_field`, `order_by_field`, `parse_single_source`
- Add imports:
```rust
use serde_json::Value;
use std::collections::HashMap;
use yard_structs::{
    AirflowJobBlock, AirflowSection, Import, Sink, Source, Transform,
};
```
- Move the 9 self-contained parsing tests: `parse_airflow_*` (6 tests) and `merge_airflow_*` (3 tests). These use only `super::*` and `serde_json::json!`.

**Update `yard-core/src/lib.rs`:**
- Remove the moved function bodies (lines 19-66 and lines 993-1276)
- Remove private helpers that moved (str_field, str_array_field, str_map_field, order_by_field, parse_single_source)
- Remove the 18 moved tests from the test module
- Add mod declarations: `pub mod config_merge;` and `pub mod parsing;`
- Add re-exports:
```rust
pub use config_merge::{build_provider_config, is_task_only, merge_provider_config};
pub use parsing::{
    merge_airflow_sections, parse_airflow_job_block, parse_airflow_section,
    parse_body, parse_create_timestamp, parse_imports, parse_job_file,
    parse_partition_by, parse_partition_timestamp_column, parse_sink,
    parse_sources, parse_transforms,
};
```
- Remove unused imports from lib.rs that were only needed by the moved code (e.g., `AirflowJobBlock`, `AirflowSection`, `Import`, `Sink`, `Source`, `Transform` — verify each is not used by remaining code before removing)
- Functions remaining in lib.rs still reference parsing/config_merge functions. Since they are re-exported via `pub use`, existing `use super::*` in the test module and direct calls within lib.rs will resolve through the re-exports. However, the remaining functions in lib.rs that call these (e.g., `make_job()` in tests calls `parse_imports()`) will need to use `crate::parsing::parse_imports` or rely on the `pub use` re-export. Since functions within lib.rs can access re-exported items directly, no changes to call sites within lib.rs are needed.

**Critical: Preserve all `.context()` and `.with_context()` error messages verbatim — do not edit, rephrase, or remove any error context strings.**
  </action>
  <verify>
    <automated>cd /Users/seanmcauliffe/Repos/yard && cargo test -p yard-core --lib && cargo check -p yard-cli -p yard-server && cargo clippy --workspace -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - yard-core/src/config_merge.rs exists and contains `pub fn is_task_only(`
    - yard-core/src/config_merge.rs contains `pub fn build_provider_config(`
    - yard-core/src/config_merge.rs contains `pub fn merge_provider_config(`
    - yard-core/src/config_merge.rs contains `#[cfg(test)]`
    - yard-core/src/parsing.rs exists and contains `pub fn parse_body(`
    - yard-core/src/parsing.rs contains `pub fn parse_sources(`
    - yard-core/src/parsing.rs contains `fn str_field(` (private helper)
    - yard-core/src/parsing.rs contains `fn parse_single_source(` (private helper)
    - yard-core/src/parsing.rs contains `#[cfg(test)]`
    - yard-core/src/lib.rs contains `pub mod config_merge;`
    - yard-core/src/lib.rs contains `pub mod parsing;`
    - yard-core/src/lib.rs contains `pub use config_merge::{build_provider_config, is_task_only, merge_provider_config}`
    - yard-core/src/lib.rs contains `pub use parsing::`
    - yard-core/src/lib.rs does NOT contain `pub fn is_task_only(`
    - yard-core/src/lib.rs does NOT contain `fn str_field(`
    - `cargo test -p yard-core --lib` exits 0
    - `cargo check -p yard-cli -p yard-server` exits 0
    - `cargo clippy --workspace -- -D warnings` exits 0
  </acceptance_criteria>
  <done>parsing.rs and config_merge.rs extracted with all their tests. lib.rs updated with mod declarations and pub use re-exports. Full workspace compiles, all tests pass, clippy clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries affected — this is a pure structural refactor with no changes to input handling, authentication, or access control.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | T (Tampering) | Module visibility | accept | All functions keep existing pub visibility; no new access paths created. Visibility tightening deferred per CONTEXT.md. |
</threat_model>

<verification>
After this plan:
- `cargo test -p yard-core --lib` — all tests pass (including 18 moved tests in new locations)
- `cargo check -p yard-cli -p yard-server` — downstream consumers compile unchanged
- `cargo clippy --workspace -- -D warnings` — zero warnings
- lib.rs line count reduced by ~350 lines (config_merge ~50 + parsing ~280 + 18 tests)
</verification>

<success_criteria>
- parsing.rs and config_merge.rs exist as standalone modules
- 18 tests (9 config_merge + 9 parsing) pass in their new module locations
- lib.rs has mod declarations and pub use re-exports for both modules
- Zero downstream compilation changes needed
- Clippy clean
</success_criteria>

<output>
After completion, create `.planning/phases/02-lib-facade/02-01-SUMMARY.md`
</output>
