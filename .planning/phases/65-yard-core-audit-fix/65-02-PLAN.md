---
phase: 65-yard-core-audit-fix
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - yard-core/src/validation/mod.rs
  - yard-core/src/validation/rules.rs
  - yard-core/src/validation/syntax.rs
  - yard-core/src/parsing.rs
autonomous: true
requirements: [OWN-01, ERR-01, MEM-01, API-01, ASYNC-01, OPT-01, NAME-01, TYPE-01, TEST-01, DOC-01, PERF-01, PROJ-01, LINT-01, ANTI-01]

must_haves:
  truths:
    - "All 4 validation/ + parsing.rs files audited against all 14 rule categories with per-file findings documented"
    - "New code since v1.12 (~208 LOC: rules.rs +58 PII entity validation, mod.rs +136 PII tests, parsing.rs +14 parse_mask_pii) receives deep per-line audit"
    - "Unchanged file (syntax.rs) verified to still hold v1.12 compliance"
    - "All violations found are fixed in committed code"
    - "cargo clippy -p yard-core --lib -- -D warnings produces zero warnings"
    - "D-06 honored: doc-examples-section (adding # Examples to public items) explicitly deferred to a future documentation phase"
    - "cargo test -p yard-core passes with no regressions"
  artifacts:
    - path: ".planning/phases/65-yard-core-audit-fix/65-02-SUMMARY.md"
      provides: "Per-file audit findings for validation/ and parsing.rs"
  key_links:
    - from: "yard-core/src/validation/rules.rs"
      to: "yard-core/src/validation/mod.rs"
      via: "validate_mask_pii called from validate_job"
      pattern: "validate_mask_pii"
    - from: "yard-core/src/parsing.rs"
      to: "yard-core/src/lib.rs"
      via: "parse_mask_pii re-exported from lib.rs"
      pattern: "pub use parsing::parse_mask_pii"
---

<objective>
Audit all 4 validation/ + parsing.rs files in yard-core against all 179 Rust coding standards (14 categories). Per D-01, this group has the second-most new code since v1.12 (PII validation rules + parse_mask_pii). Per D-03, new code receives deep per-line audit; unchanged files get lightweight re-verification. Per D-04, fix anything contained within yard-core that does not change public API signatures or wire format.

Purpose: Ensure the validation and parsing subsystems are fully compliant with all coding standards, with particular attention to the PII validation additions from v1.14.
Output: Per-file audit findings documented in SUMMARY. Any violations fixed and committed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/65-yard-core-audit-fix/65-RESEARCH.md
@.planning/phases/65-yard-core-audit-fix/65-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deep audit validation/rules.rs and validation/mod.rs</name>
  <files>yard-core/src/validation/rules.rs, yard-core/src/validation/mod.rs</files>
  <read_first>
    yard-core/src/validation/rules.rs
    yard-core/src/validation/mod.rs
    .planning/phases/65-yard-core-audit-fix/65-RESEARCH.md (Known Violations, Non-Violations N-03/N-04, Changes Since v1.12 sections 5-6)
  </read_first>
  <action>
Deep audit these 2 files against all 14 rule categories.

validation/rules.rs (+58 LOC new PII entity validation):
- ERR: Confirm the expect("static regex") on line ~44 has the allow(clippy::expect_used) attribute and is valid per err-expect-bugs-only (static regex compilation -- programming error if fails). Research confirmed this as N-03.
- MEM: Confirm HashSet::with_capacity(job.mask_pii.len()) is used for duplicate detection. Confirm format! usage for per-error field paths (e.g. mask_pii[0]) is acceptable -- validation is not a hot path per research N-04.
- NAME: Confirm SCREAMING_SNAKE_RE constant follows SCREAMING_SNAKE_CASE naming convention.
- OWN: Check that the LazyLock static regex does not involve unnecessary cloning.
- All other categories: Full check against the existing ~563 LOC of validation rules.

validation/mod.rs (+136 LOC, all in test module):
- TEST: Verify the 5 new PII validation tests follow cfg(test), use super::*, descriptive names, and AAA pattern.
- DOC: Verify //! module doc present and /// on public validate_job, validate_dag functions.
- ERR: Verify all public functions return anyhow::Result with proper context chaining.
- All other categories: Full check on the existing ~280 prod LOC.

Fix any violations found per D-04. Document all findings for the SUMMARY.
  </action>
  <verify>
    <automated>cd /Users/seanmcauliffe/Repos/yard-project/yard && cargo clippy -p yard-core --lib -- -D warnings 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - validation/rules.rs audited: SCREAMING_SNAKE_RE uses LazyLock with allow(clippy::expect_used) attribute, HashSet::with_capacity present for duplicate detection, format! in error paths confirmed as non-hot-path usage
    - validation/mod.rs audited: //! module doc present, validate_job and validate_dag have /// docs with Errors section, 5 new PII tests follow cfg(test) + use super::* + descriptive names
    - All 14 rule categories checked for each file with findings documented
    - cargo clippy -p yard-core --lib -- -D warnings returns zero warnings
  </acceptance_criteria>
  <done>Both validation files audited against 14 categories. Findings documented. Any violations fixed.</done>
</task>

<task type="auto">
  <name>Task 2: Audit parsing.rs and validation/syntax.rs + gate checks</name>
  <files>yard-core/src/parsing.rs, yard-core/src/validation/syntax.rs</files>
  <read_first>
    yard-core/src/parsing.rs
    yard-core/src/validation/syntax.rs
    .planning/phases/65-yard-core-audit-fix/65-RESEARCH.md (Changes Since v1.12 section 7 for parsing.rs)
  </read_first>
  <action>
Audit these 2 files against all 14 rule categories.

parsing.rs (+14 LOC: parse_mask_pii function):
- Deep audit the new parse_mask_pii function: must_use attribute present, returns Vec of String, uses idiomatic Option chaining with and_then/map/filter_map/unwrap_or_default. No Errors doc section needed (returns Vec, not Result).
- Verify the rest of the file (681 prod LOC) still holds v1.12 compliance: //! module doc, /// on public items, Errors sections on fallible functions, no prod unwrap, proper error propagation with ?.

validation/syntax.rs (61 LOC, no changes since v1.12):
- Lightweight re-verify: confirm //! module doc, /// docs, no prod unwrap, proper pub(crate) or pub(super) visibility.

After audit, run full gate checks:
- cargo clippy -p yard-core --lib -- -D warnings (zero warnings)
- cargo test -p yard-core (all tests pass)

Fix any violations found per D-04. Document all findings for the SUMMARY.
  </action>
  <verify>
    <automated>cd /Users/seanmcauliffe/Repos/yard-project/yard && cargo clippy -p yard-core --lib -- -D warnings 2>&1 | head -20 && cargo test -p yard-core 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - parsing.rs audited: parse_mask_pii has must_use attribute, uses and_then/map/filter_map chaining (no manual unwrap), //! module doc present, all public parsing functions have /// docs with Errors sections
    - validation/syntax.rs re-verified: //! module doc present, /// on public items, no prod unwrap
    - cargo clippy -p yard-core --lib -- -D warnings returns zero warnings
    - cargo test -p yard-core output shows zero failures
  </acceptance_criteria>
  <done>All 4 validation/ + parsing.rs files audited. Zero clippy warnings. All tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| YAML config input -> validation | Untrusted YAML job definitions validated against schema and semantic rules |
| YAML config input -> parsing | Untrusted YAML deserialized into typed Rust structs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-65-03 | Tampering | validation/rules.rs validate_mask_pii | low | mitigate | SCREAMING_SNAKE_CASE regex validates PII entity type format; rejects malformed input before it reaches codegen |
| T-65-04 | Tampering | parsing.rs parse_mask_pii | low | accept | Uses serde typed deserialization from YAML; malformed input triggers serde error before reaching parse_mask_pii. parse_mask_pii only processes already-deserialized serde_yaml::Value. |
</threat_model>

<verification>
- `cargo clippy -p yard-core --lib -- -D warnings` returns zero warnings
- `cargo test -p yard-core` passes all tests with zero failures
- All 4 validation/ + parsing.rs files have per-file audit findings documented in SUMMARY
</verification>

<success_criteria>
All 4 validation/ and parsing.rs source files are verified compliant against all 179 Rust coding standards (14 categories). Any violations found during audit are fixed. Gate checks pass.
</success_criteria>

## Artifacts this phase produces

This plan is an audit -- it produces findings documentation, not new symbols. Files potentially modified:
- `yard-core/src/validation/rules.rs` -- only if violations found (research predicts none)
- `yard-core/src/validation/mod.rs` -- only if violations found (research predicts none)
- `yard-core/src/validation/syntax.rs` -- only if violations found (research predicts none)
- `yard-core/src/parsing.rs` -- only if violations found (research predicts none)
- `.planning/phases/65-yard-core-audit-fix/65-02-SUMMARY.md` -- audit findings

<output>
Create `.planning/phases/65-yard-core-audit-fix/65-02-SUMMARY.md` when done
</output>
