---
phase: 65-yard-core-audit-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - yard-core/src/codegen/mod.rs
  - yard-core/src/codegen/helpers.rs
  - yard-core/src/codegen/pii.rs
  - yard-core/src/codegen/sink.rs
  - yard-core/src/codegen/source.rs
  - yard-core/src/codegen/transform.rs
autonomous: true
requirements: [OWN-01, ERR-01, MEM-01, API-01, ASYNC-01, OPT-01, NAME-01, TYPE-01, TEST-01, DOC-01, PERF-01, PROJ-01, LINT-01, ANTI-01]

must_haves:
  truths:
    - "All 6 codegen/ files audited against all 14 rule categories with per-file findings documented"
    - "New code since v1.12 (~319 LOC: pii.rs, mod.rs PII+Iceberg, helpers.rs needs_pii_imports, sink.rs column reorder) receives deep per-line audit"
    - "Unchanged files (source.rs, transform.rs) verified to still hold v1.12 compliance"
    - "All violations found are fixed in committed code"
    - "cargo clippy -p yard-core --lib -- -D warnings produces zero warnings"
    - "D-06 honored: doc-examples-section (adding # Examples to public items) explicitly deferred to a future documentation phase"
    - "cargo test -p yard-core passes with no regressions"
  artifacts:
    - path: ".planning/phases/65-yard-core-audit-fix/65-01-SUMMARY.md"
      provides: "Per-file audit findings for codegen/"
  key_links:
    - from: "yard-core/src/codegen/pii.rs"
      to: "yard-core/src/codegen/mod.rs"
      via: "render_pii imported and called from generate_script"
      pattern: "use pii::render_pii"
    - from: "yard-core/src/codegen/helpers.rs"
      to: "yard-core/src/codegen/mod.rs"
      via: "needs_pii_imports called for conditional import generation"
      pattern: "needs_pii_imports"
---

<objective>
Audit all 6 codegen/ files in yard-core against all 179 Rust coding standards (14 categories: OWN, ERR, MEM, API, ASYNC, OPT, NAME, TYPE, TEST, DOC, PERF, PROJ, LINT, ANTI). Per D-01, codegen/ has the most new code since v1.12 (PII codegen + Iceberg column reorder). Per D-03, new code receives deep per-line audit; unchanged files get lightweight re-verification. Per D-04, fix anything contained within yard-core that does not change public API signatures or wire format.

Purpose: Ensure the codegen subsystem -- the most-changed area since the last audit -- is fully compliant with all coding standards.
Output: Per-file audit findings documented in SUMMARY. Any violations fixed and committed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/65-yard-core-audit-fix/65-RESEARCH.md
@.planning/phases/65-yard-core-audit-fix/65-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deep audit codegen/pii.rs, codegen/helpers.rs, codegen/mod.rs</name>
  <files>yard-core/src/codegen/pii.rs, yard-core/src/codegen/helpers.rs, yard-core/src/codegen/mod.rs</files>
  <read_first>
    yard-core/src/codegen/pii.rs
    yard-core/src/codegen/helpers.rs
    yard-core/src/codegen/mod.rs
    .planning/phases/65-yard-core-audit-fix/65-RESEARCH.md (Known Violations and Non-Violations sections)
  </read_first>
  <action>
Deep audit these 3 files against all 14 rule categories. These files contain the most new code since v1.12 (~311 LOC: pii.rs is entirely new at 151 LOC, mod.rs has +294 LOC of PII integration and Iceberg reorder tests, helpers.rs has +17 LOC for needs_pii_imports).

For each file, check all 14 categories systematically:
- OWN: borrow-over-clone, slice-over-vec, no unnecessary cloning
- ERR: Result returns with context, no prod unwrap, question-mark propagation
- MEM: with_capacity where size known, write!/writeln! over format! where appropriate, no format! on hot paths
- API: must_use on pure functions, non_exhaustive on cross-crate enums, common traits derived
- ASYNC: no lock-across-await (N/A for codegen -- synchronous module)
- OPT: inline on small hot functions where warranted
- NAME: CamelCase types, snake_case functions, SCREAMING_SNAKE constants
- TYPE: enums for states, Option for nullable, no stringly-typed abuse
- TEST: cfg(test) modules, use super::*, descriptive names, AAA pattern
- DOC: module-level //! docs, /// on public items, Errors section on fallible functions
- PERF: iterators over indexing, no unnecessary collect(), lazy iteration
- PROJ: pub(super)/pub(crate) for internal APIs, feature-based modules
- LINT: workspace lint compliance (correctness=deny, suspicious=deny)
- ANTI: no unwrap abuse, no excessive cloning, no format! on hot paths

Research pre-findings to verify:
- pii.rs: String::with_capacity(256), write!/writeln!, pub(super), must_use, complete docs -- confirm these hold
- helpers.rs: needs_pii_imports has inline, must_use, complete docs -- confirm
- mod.rs: PII integration follows existing patterns, test names descriptive -- confirm

Fix any violations found per D-04. Document all findings (violations and confirmed-compliant areas) for the SUMMARY.
  </action>
  <verify>
    <automated>cd /Users/seanmcauliffe/Repos/yard-project/yard && cargo clippy -p yard-core --lib -- -D warnings 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - codegen/pii.rs audited: String::with_capacity(256) present, write!/writeln! used (not format!), pub(super) visibility on render_pii, must_use attribute present, //! module doc present, /// function doc present
    - codegen/helpers.rs audited: needs_pii_imports has inline attribute, must_use attribute, /// doc with description
    - codegen/mod.rs audited: PII block insertion follows existing indentation/pattern, test module has cfg(test) + allow(clippy::unwrap_used) + use super::*, new test names are descriptive
    - All 14 rule categories checked for each file with findings documented
    - cargo clippy -p yard-core --lib -- -D warnings returns zero warnings
  </acceptance_criteria>
  <done>All 3 deep-audit codegen files checked against 14 categories. Findings documented. Any violations fixed.</done>
</task>

<task type="auto">
  <name>Task 2: Audit codegen/sink.rs, codegen/source.rs, codegen/transform.rs + gate checks</name>
  <files>yard-core/src/codegen/sink.rs, yard-core/src/codegen/source.rs, yard-core/src/codegen/transform.rs</files>
  <read_first>
    yard-core/src/codegen/sink.rs
    yard-core/src/codegen/source.rs
    yard-core/src/codegen/transform.rs
    .planning/phases/65-yard-core-audit-fix/65-RESEARCH.md (Non-Violations N-08 for sink.rs format! usage)
  </read_first>
  <action>
Audit these 3 files against all 14 rule categories:

codegen/sink.rs: Deep audit on the +8 LOC Iceberg column reorder addition. Per research N-08, the format! for column_order Python code string is template construction, not a hot path -- confirm this assessment. Verify the rest of the file still holds v1.12 compliance (206 prod LOC).

codegen/source.rs: Lightweight re-verify (196 prod LOC, no changes since v1.12). Confirm //! module doc, /// on public items, no prod unwrap, write!/writeln! usage, pub(super) visibility pattern.

codegen/transform.rs: Lightweight re-verify (194 prod LOC, no changes since v1.12). Same checks as source.rs.

For all 3 files, check the same 14 categories as Task 1. Fix any violations per D-04. Document findings.

After audit, run the full gate checks:
- cargo clippy -p yard-core --lib -- -D warnings (zero warnings)
- cargo test -p yard-core (all tests pass)
  </action>
  <verify>
    <automated>cd /Users/seanmcauliffe/Repos/yard-project/yard && cargo clippy -p yard-core --lib -- -D warnings 2>&1 | head -20 && cargo test -p yard-core 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - codegen/sink.rs audited: format! in Iceberg column reorder confirmed as template construction (not hot path), //! module doc present, pub(super) visibility, no prod unwrap
    - codegen/source.rs re-verified: //! module doc present, /// on public items, write!/writeln! usage confirmed, pub(super) visibility, no prod unwrap
    - codegen/transform.rs re-verified: same checks as source.rs all confirmed
    - cargo clippy -p yard-core --lib -- -D warnings returns zero warnings
    - cargo test -p yard-core output shows zero failures
  </acceptance_criteria>
  <done>All 6 codegen/ files audited. Zero clippy warnings on --lib. All yard-core tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| YAML config input -> codegen | Untrusted YAML job definitions are parsed and used to generate PySpark scripts |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-65-01 | Tampering | codegen/mod.rs generate_script | medium | accept | YAML input is validated by validation/ module before reaching codegen; codegen trusts validated input. Audit-only phase does not change this architecture. |
| T-65-02 | Information Disclosure | codegen/pii.rs render_pii | low | accept | PII masking codegen generates EntityDetector calls -- the generated code runs in customer's Glue environment, not in yard itself. No secrets handled by yard. |
</threat_model>

<verification>
- `cargo clippy -p yard-core --lib -- -D warnings` returns zero warnings
- `cargo test -p yard-core` passes all tests with zero failures
- All 6 codegen/ files have per-file audit findings documented in SUMMARY
</verification>

<success_criteria>
All 6 codegen/ source files are verified compliant against all 179 Rust coding standards (14 categories). Any violations found during audit are fixed. Gate checks pass.
</success_criteria>

## Artifacts this phase produces

This plan is an audit -- it produces findings documentation, not new symbols. Files potentially modified:
- `yard-core/src/codegen/pii.rs` -- only if violations found (research predicts none)
- `yard-core/src/codegen/helpers.rs` -- only if violations found (research predicts none)
- `yard-core/src/codegen/mod.rs` -- only if violations found (research predicts none)
- `yard-core/src/codegen/sink.rs` -- only if violations found (research predicts none)
- `yard-core/src/codegen/source.rs` -- only if violations found (research predicts none)
- `yard-core/src/codegen/transform.rs` -- only if violations found (research predicts none)
- `.planning/phases/65-yard-core-audit-fix/65-01-SUMMARY.md` -- audit findings

<output>
Create `.planning/phases/65-yard-core-audit-fix/65-01-SUMMARY.md` when done
</output>
