---
phase: 70-core-slimming-documentation
plan: 02
type: execute
wave: 2
depends_on: [70-01]
files_modified:
  - yard-core/src/diff.rs
  - yard-core/src/orchestrate.rs
  - yard-core/src/show.rs
  - yard-core/src/validation/mod.rs
  - yard-core/src/validation/rules.rs
  - yard-core/src/config_merge.rs
  - yard-core/src/list_targets.rs
  - yard-cli/src/commands/show.rs
  - yard-cli/src/commands/list.rs
  - yard-cli/src/commands/display.rs
  - yard-cli/src/commands/plan.rs
  - yard-cli/src/commands/apply.rs
autonomous: true
requirements: [SLIM-02]

must_haves:
  truths:
    - "cargo test --workspace passes with zero failures"
    - "cargo clippy --workspace -- -D warnings passes with zero warnings"
    - "No remaining references to codegen::, airflow_dag::, dag_lifecycle::, or DagDiff in the workspace"
    - "calculate_diff is async and uses plugin codegen instead of built-in codegen"
    - "orchestrate plan/apply/destroy have no DAG handling"
    - "validation has no provider-specific checks or codegen calls"
    - "CLI commands have no DAG-related code paths"
  artifacts:
    - yard-core/src/diff.rs (async calculate_diff using Provider::codegen)
    - yard-core/src/orchestrate.rs (no DAG handling, no direct codegen calls)
    - yard-core/src/show.rs (async show using Provider::codegen, no DAG functions)
    - yard-core/src/validation/mod.rs (no DAG validation, no codegen call)
    - yard-core/src/config_merge.rs (no is_task_only function)
    - yard-core/src/list_targets.rs (no DAG target enumeration)
  key_links:
    - "calculate_diff -> Provider::codegen -> hash computation (diff correctness)"
    - "orchestrate::plan -> calculate_diff (plan output)"
    - "orchestrate::apply -> calculate_diff (deploy trigger)"
---

<objective>
Rewire all remaining yard-core modules and CLI commands to remove references to deleted codegen/airflow_dag/dag_lifecycle modules and adapt to the plugin-only architecture. Make calculate_diff async for plugin codegen. Remove DAG handling from orchestrate, show, validation, list_targets, and all CLI commands. Completes SLIM-02 (all compiled-in provider code removed from yard-core).

Purpose: After Plan 01 deleted the modules, the remaining files still reference them via use/import statements and function calls. This plan eliminates all dangling references so the full workspace compiles and all tests pass.

Output: Fully compiling workspace with zero references to deleted modules. cargo test --workspace green. cargo clippy clean.
</objective>

<artifacts_produced>
## Artifacts this phase produces

### Modified functions
- `calculate_diff()` (yard-core/src/diff.rs) — now async, accepts &PluginHostConfig, uses get_provider_for_job + Provider::codegen instead of codegen::generate_python_script
- `plan()` (yard-core/src/orchestrate.rs) — no DAG handling, no dag_diffs on PlanResult, awaits calculate_diff
- `apply()` (yard-core/src/orchestrate.rs) — no DAG handling, no dag_diffs on ApplyResult, awaits calculate_diff
- `destroy_all()` / `destroy_job()` (yard-core/src/orchestrate.rs) — no DAG destruction calls
- `show()` (yard-core/src/show.rs) — now async, uses plugin codegen via get_provider_for_job
- `validate_job_full()` / `validate_job_full_with_schema()` (yard-core/src/validation/mod.rs) — no codegen call, no DAG validation
- `validate_job()` (yard-core/src/validation/rules.rs) — no provider-specific match arms for Bash/Glue/Emr
- `list_targets()` (yard-core/src/list_targets.rs) — no DAG target enumeration
- `is_task_only()` removed from config_merge.rs

### Removed struct fields
- `PlanResult.dag_diffs`, `PlanResult.dag_required_connections`, `PlanResult.dag_warnings`
- `ApplyResult.dag_created`, `ApplyResult.dag_modified`, `ApplyResult.dag_deleted`
- `DagApplyResult`, `DagDestroyResult` (already deleted in Plan 01 via dag_lifecycle.rs)

### Removed functions
- `show_dag()`, `show_dag_with_state()` from show.rs
- `validate_dag_full()`, `validate_project()` from validation/mod.rs
- `is_task_only()` from config_merge.rs
- DAG-related helper functions in validation/mod.rs (check_mutual_exclusion, check_empty_composites, etc.)

### CLI changes
- show.rs — remove DAG show fallback, handle async show
- list.rs — remove parse_account_from_role_arn import, remove DAG diffs
- display.rs — remove DagDiff rendering
- plan.rs — update for PlanResult without dag_diffs
- apply.rs — update for ApplyResult without dag fields
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-core-slimming-documentation/70-CONTEXT.md
@.planning/phases/70-core-slimming-documentation/70-RESEARCH.md
@.planning/phases/70-core-slimming-documentation/70-PATTERNS.md
@.planning/phases/70-core-slimming-documentation/70-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire yard-core modules for plugin-only architecture</name>
  <files>
    yard-core/src/diff.rs,
    yard-core/src/orchestrate.rs,
    yard-core/src/show.rs,
    yard-core/src/validation/mod.rs,
    yard-core/src/validation/rules.rs,
    yard-core/src/config_merge.rs,
    yard-core/src/list_targets.rs
  </files>
  <read_first>
    - yard-core/src/diff.rs (current calculate_diff at line 33 -- sync codegen call at line 38)
    - yard-core/src/orchestrate.rs (use statements lines 14-35, plan/apply/destroy functions -- identify all codegen/airflow_dag/dag_lifecycle references)
    - yard-core/src/show.rs (show_dag/show_dag_with_state/show functions -- entire file)
    - yard-core/src/validation/mod.rs (use crate::airflow_dag at line 28, codegen call at line 59, validate_dag_full, validate_project, all DAG helpers)
    - yard-core/src/validation/rules.rs (JobType::Glue/Emr/Bash match arms, mask_pii Glue-only check)
    - yard-core/src/config_merge.rs (is_task_only function at line 21)
    - yard-core/src/list_targets.rs (airflow_dag and dag_lifecycle imports at lines 20-22, DAG enumeration in list_targets)
    - yard-core/src/providers/mod.rs (get_provider_for_job signature after Plan 01 -- need the exact parameters to call from diff.rs)
    - yard-core/src/plugin_host/mod.rs (PluginHostConfig re-export)
    - .planning/phases/70-core-slimming-documentation/70-RESEARCH.md (Pattern 3: Diff restructuring, Pitfalls 5-8)
  </read_first>
  <action>
**diff.rs -- Make calculate_diff async (RESEARCH Pattern 3, Option A per assumption A1):**
- Change calculate_diff signature to async fn. Add parameters: plugin_host_config: &PluginHostConfig. Import PluginHostConfig from crate::plugin_host.
- Remove use crate::codegen import.
- Replace codegen::generate_python_script(name, job_def) call with plugin codegen:
  1. Call crate::providers::get_provider_for_job(&job_def.job_type, &job_def.config, job_def.plugin_version.as_deref(), job_def.plugin_source.as_deref(), plugin_host_config).await
  2. Call provider.codegen(name, &job_def.config).await
  3. Use the returned Option String, defaulting to empty string via .unwrap_or_default()
- Keep all hash computation logic unchanged (config_str, trigger_str, combined hash)
- Update tests: test fixtures that use JobType::Bash must use JobType::Plugin("bash".to_string()). Tests that call calculate_diff must become async (#[tokio::test]) and pass a PluginHostConfig. Since tests won't have real plugin binaries, consider: either remove the diff tests that relied on codegen producing real output (they tested codegen behavior, not diff logic), or provide a mock/test-fixture approach. The simplest approach: remove the codegen-dependent parts of diff tests and keep the hash-stability tests by mocking the script content. Or keep the tests but make them work with the fact that plugin codegen will fail (and handle that gracefully).

Actually, the simpler approach for diff tests: since calculate_diff now requires a plugin binary to generate scripts, and test fixtures don't have plugin binaries, the existing diff tests (DIFF-01, HASH-01, HASH-02) can no longer call calculate_diff directly. These tests tested codegen+diff integration; with codegen moved to plugins, they belong in plugin integration tests. Remove them. The core diff logic (BTreeMap ordering, hash comparison, Create/Modify/Delete classification) is exercised through orchestrate-level integration tests.

**orchestrate.rs -- Remove DAG handling, update codegen path (D-07):**
- Remove use crate::airflow_dag import
- Remove use crate::codegen import
- Remove use crate::dag_lifecycle::{apply_dags, destroy_all_dags} import (and the dag_lifecycle use statement from the top-level imports)
- Remove use crate::config_merge::is_task_only import
- Remove DagDiff from the yard_structs import (if present)
- Remove dag_diffs/dag_created/dag_modified/dag_deleted/dag_required_connections/dag_warnings fields from PlanResult and ApplyResult structs
- In plan(): remove all DAG collection (airflow_dag::collect_dags), DAG validation (validate_dag_full), DAG diff (calculate_dag_diffs), DAG warning (validate_project) logic. Remove is_task_only guards around codegen calls. Update calculate_diff call to be .await (it is now async). Pass plugin_host_config to calculate_diff.
- In apply(): same DAG removal. Remove apply_dags calls. Remove is_task_only guards. Update calculate_diff to .await. Remove DAG result fields from ApplyResult construction.
- In destroy_all() / destroy_job(): remove destroy_all_dags / destroy_dag calls if any exist.
- Update verify_deployed_resources if it references is_task_only or DAG types.
- In the get_provider_for_job call sites: update to pass &job_def.job_type (by reference, since Copy is gone) and include plugin_version/plugin_source from job_def.
- Update test helper make_job (around line 817) to use JobType::Plugin("glue".to_string()) instead of JobType::Glue.
- Remove test code that references DagDiff, dag_diffs, or DAG types.

**show.rs -- Remove DAG functions, make show async (D-07):**
- Remove use crate::airflow_dag and use crate::dag_lifecycle imports
- Delete show_dag() function entirely
- Delete show_dag_with_state() function entirely
- Make show() async: it now calls get_provider_for_job to get a provider, then calls provider.codegen(job_name, &job_def.config).await. Returns the script or an error. Requires plugin_host_config as parameter. Import PluginHostConfig.

**validation/mod.rs -- Remove DAG validation and codegen call (Pitfall 5, D-08):**
- Remove use crate::airflow_dag::ResolvedDag import
- Remove the codegen::generate_python_script call in validate_job_full_with_schema (lines 59-70). Without codegen in core, script syntax checking moves to the plugin. validate_job_full_with_schema simply returns the schema validation errors without script generation.
- Delete validate_dag_full() function and all its helpers (check_mutual_exclusion, check_empty_composites, check_heterogeneous_any, check_reserved_task_ids, check_max_active_runs, check_s3_poke_interval)
- Delete validate_project() function
- Remove all DAG-related tests from the #[cfg(test)] module
- Remove tests that depend on codegen (validate_job_full_catches_bad_body, validate_job_full_passes_valid_job, validate_job_full_catches_bad_job_file, validate_job_full_passes_good_job_file, bash_passes_validate_job_full) since validate_job_full no longer calls codegen
- Keep validate_python_syntax function and its tests (it's a standalone utility)
- Keep validate_job and validate_job_with_schema exports

**validation/rules.rs -- Remove provider-specific validation arms (D-08):**
- Remove provider-specific match arms on JobType::Glue, JobType::Emr, JobType::Bash throughout validate_job
- The mask_pii Glue-only check (matches!(job_def.job_type, JobType::Glue)) becomes a no-op or is removed -- mask_pii validation is provider-specific and belongs in the plugin. Remove the mask_pii job-type gate and the is_task_only early return.
- The bash-specific validation (command required, rejects sources/sink/transforms/body) is removed -- Bash is now a plugin type, its validation is plugin-specific.
- Remove references to JobType::Bash, JobType::Glue, JobType::Emr in match arms. Since JobType is now Plugin(String), the match becomes let JobType::Plugin(type_name) = &job_def.job_type.
- Remove tests that tested Bash-specific validation (bash_valid_with_command, bash_missing_command_errors, etc.) and Glue config validation (valid_glue_config_passes, invalid_worker_type, etc.) -- these test provider-specific validation that now belongs in plugins.
- Keep structural validation (sources, transforms, sinks, source/sink required fields, reference checking) -- these are yard's structural schema per D-04/D-08.
- Update remaining test fixtures: minimal_job() and valid_glue_job() should use JobType::Plugin("glue".to_string()) instead of JobType::Glue.

**config_merge.rs -- Remove is_task_only (Pitfall 8, assumption A2):**
- Remove the is_task_only() function entirely. In v2.0, the plugin decides whether a job type needs codegen/deploy.
- Remove JobType import if no longer needed (keep it if build_provider_config references it).
- Remove is_task_only tests.
- Keep build_provider_config() and merge_provider_config() -- these are still needed for config cascade.

**list_targets.rs -- Remove DAG target enumeration (Pitfall 7, assumption A3):**
- Remove use crate::airflow_dag and use crate::dag_lifecycle imports
- Remove use crate::airflow_dag::parse_account_from_role_arn import
- Remove the entire DAG enumeration block in list_targets() (the section that calls airflow_dag::collect_dags and iterates dag_lifecycle::resolve_effective_dag_aws). Only job targets remain.
- If parse_account_from_role_arn is still needed for job-level assume_role parsing, relocate it to a local helper in list_targets.rs or utils.rs. Check: does assume_role_str already handle the parsing? Looking at the code -- assume_role_str extracts the ARN string, but parse_account_from_role_arn extracts the 12-digit account ID from the ARN. If job targets still need the account ID extraction, keep a local copy. The function is simple (~5 lines of string splitting).
- Update test fixtures: make_manifest uses JobType::Glue -- change to JobType::Plugin("glue".to_string()).
  </action>
  <verify>
    <automated>cd /Users/seanmcauliffe/Repos/yard-project/yard && cargo check -p yard-core --lib && cargo test -p yard-core --lib 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -rn "crate::codegen" yard-core/src/ returns no results (excluding deleted files)
    - grep -rn "crate::airflow_dag" yard-core/src/ returns no results
    - grep -rn "crate::dag_lifecycle" yard-core/src/ returns no results
    - grep -rn "is_task_only" yard-core/src/ returns no results
    - grep -c "pub fn validate_dag_full" yard-core/src/validation/mod.rs == 0
    - grep -c "pub fn validate_project" yard-core/src/validation/mod.rs == 0
    <!-- planner-discipline-allow: show_dag -->
    - grep -c "show_dag" yard-core/src/show.rs == 0
    - calculate_diff in diff.rs has async fn signature
    - cargo check -p yard-core --lib passes
    - cargo test -p yard-core --lib passes
  </acceptance_criteria>
  <done>All yard-core modules rewired. No references to deleted codegen/airflow_dag/dag_lifecycle modules. calculate_diff is async. Validation has no provider-specific checks. list_targets has no DAG enumeration.</done>
</task>

<task type="auto">
  <name>Task 2: Update CLI commands and verify full workspace compilation</name>
  <files>
    yard-cli/src/commands/show.rs,
    yard-cli/src/commands/list.rs,
    yard-cli/src/commands/display.rs,
    yard-cli/src/commands/plan.rs,
    yard-cli/src/commands/apply.rs
  </files>
  <read_first>
    - yard-cli/src/commands/show.rs (yard_core::airflow_dag::collect_dags and yard_core::show_dag_with_state references)
    - yard-cli/src/commands/list.rs (yard_core::airflow_dag::parse_account_from_role_arn and dag_diffs references)
    - yard-cli/src/commands/display.rs (DagDiff rendering code)
    - yard-cli/src/commands/plan.rs (dag_diffs field access on PlanResult)
    - yard-cli/src/commands/apply.rs (dag_diffs field access on ApplyResult)
    - yard-core/src/orchestrate.rs (PlanResult and ApplyResult struct definitions after Task 1 modifications)
  </read_first>
  <action>
**show.rs (CLI) -- Remove DAG show fallback (Pitfall 6):**
- Remove import of yard_core::airflow_dag::collect_dags (or similar DAG collection import)
- Remove the DAG show fallback code path (the branch that tries to show a DAG when a job is not found)
- Update the show job path to call yard_core::show() which is now async -- use .await
- The show command may need to construct a PluginHostConfig to pass to the async show function. Check what default PluginHostConfig construction looks like (from plugin_host/mod.rs). The CLI should construct it from the project root (.yard/plugins/ directory).

**list.rs (CLI) -- Remove DAG-related code (Pitfall 6):**
- Remove yard_core::airflow_dag::parse_account_from_role_arn import (if CLI imported it directly)
- Remove any dag_diffs display code
- The list targets output should now only contain job targets, no DAG targets

**display.rs (CLI) -- Remove DagDiff rendering (Pitfall 6):**
- Remove DagDiff import from yard_structs
- Remove the DagDiff rendering/display code block
- Keep job diff rendering unchanged

**plan.rs (CLI) -- Update for no dag_diffs (Pitfall 6):**
- Remove dag_diffs field access from PlanResult
- Remove dag_required_connections display
- Remove dag_warnings display
- The plan output should show only job diffs

**apply.rs (CLI) -- Update for no DAG fields (Pitfall 6):**
- Remove dag_diffs field access from ApplyResult
- Remove dag_created/dag_modified/dag_deleted summary display
- The apply output should show only job operations

After all CLI updates, run the full workspace check:
- cargo check --workspace
- cargo test --workspace
- cargo clippy --workspace -- -D warnings

Fix any remaining compilation errors. Common issues:
- JobType by-value patterns that need .clone() or &JobType
- Missing imports after removing DAG types
- Test fixtures using old JobType variants
  </action>
  <verify>
    <automated>cd /Users/seanmcauliffe/Repos/yard-project/yard && cargo test --workspace 2>&1 | tail -10 && cargo clippy --workspace -- -D warnings 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -rn "DagDiff" yard-cli/src/ returns no results
    - grep -rn "dag_diffs" yard-cli/src/ returns no results
    - grep -rn "airflow_dag" yard-cli/src/ returns no results
    - grep -rn "show_dag" yard-cli/src/ returns no results
    - cargo test --workspace passes (all remaining tests green)
    - cargo clippy --workspace -- -D warnings passes (zero warnings)
    - cargo tree -p yard-core 2>/dev/null | grep -cE "aws-sdk-(glue|emr)" == 0
  </acceptance_criteria>
  <done>Full workspace compiles. All tests pass. Clippy clean. No references to deleted modules anywhere in the workspace. aws-sdk-glue and aws-sdk-emr absent from dependency tree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| job config -> diff computation | Plugin codegen output feeds hash computation via async Provider::codegen |
| CLI -> yard-core | CLI consumes PlanResult/ApplyResult (DAG fields removed) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-70-04 | Tampering | calculate_diff async codegen | medium | mitigate | Plugin codegen is already integrity-checked via SHA-256 binary verification (Phase 69 DIST-02). The diff hash computation itself is unchanged -- only the source of script content changes from built-in to plugin. |
| T-70-05 | Repudiation | Removed validation rules | low | accept | Provider-specific validation (worker types, bookmark values) moves to plugin schema() and validate() operations, which are already logged via stderr. Core structural validation remains unchanged and is not weakened. |
| T-70-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages installed. Only removing dead code and updating call sites. |
</threat_model>

<verification>
- cargo test --workspace passes with zero failures
- cargo clippy --workspace -- -D warnings passes with zero warnings
- grep -rn "codegen::" yard-core/src/ finds no references (excluding test comments)
- grep -rn "airflow_dag" yard-core/src/ and yard-cli/src/ finds no references
- grep -rn "DagDiff" yard-cli/src/ finds no references
- cargo tree -p yard-core shows no aws-sdk-glue or aws-sdk-emr
</verification>

<success_criteria>
Full workspace compiles, all tests pass, clippy clean. Zero references to deleted codegen/airflow_dag/dag_lifecycle modules anywhere in the workspace. CLI commands output job-only results without DAG handling.
</success_criteria>

<output>
Create `.planning/phases/70-core-slimming-documentation/70-02-SUMMARY.md` when done
</output>
