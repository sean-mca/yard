# Account-level context. Required by yard's hierarchical resolver
# (yard-core/src/resolve.rs::load_context). Content is opaque YAML —
# yard reads it for ${account.*} variable interpolation, but does not
# enforce a schema on it.
name: dev
