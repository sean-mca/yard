// Declare the submodules located in the commands/ directory
pub mod init;
// pub mod apply;
// pub mod plan;
// pub mod destroy;

// You can also use "pub use" here if you want to flatten the API
// so lib.rs can call commands::init() instead of commands::init::execute()
